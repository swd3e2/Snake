#pragma once

#include "Graphics/RenderQueue/RenderQueue.h"
#include "Graphics/Renderer.h"
#include "Components.h"

class GBufferModelRenderer {
public:
	void render(Renderer* renderer, RenderQueue* renderQueue, TransformComponent* transformComponent, RenderComponent* renderComponent)
	{

	}
};